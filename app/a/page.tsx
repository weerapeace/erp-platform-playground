"use client";

// หน้าอนุมัติเล็ก — Endpoint ของ LIFF (/a) · อ่าน token จาก ?token= หรือ liff.state
import { ApproveView } from "./approve-view";

export default function ApprovePage() {
  return <ApproveView />;
}
