"use client";

// หน้า /tasks/content-calendar — ปฏิทินคอนเทนต์ (เนื้อหาจริงใน content-calendar.tsx)
import { ContentCalendarView } from "./content-calendar";

export default function ContentCalendarPage() {
  return <ContentCalendarView />;
}
